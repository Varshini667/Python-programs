lst = []
num = int(input('How many numbers: '))
for n in range(num):
    numbers = int(input('Enter number '))
    lst.append(numbers)
print(" second Maximum element in the list is :", max(lst), "\n second Minimum element in the list is :", min(lst))
